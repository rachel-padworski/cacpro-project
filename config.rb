# http_path = "/" #root level target path
# css_dir - "." #targets the default style.css file at the root level of the theme
# sass_dir = "scss" #targets the scss directory
# images_dir = "images" #targets the pre-existing image directory
# javascripts_dir = "js" #targets the JavaScript directory

# # You can select your preferred output style here (can be overridden via the command line):
# # output_style = :expanded or :nested or :compact or :compressed
# output_style = :nested
 

# # To enable relative paths to assets via compass helper functions.
# # note: this is important in wordpress themes for sprites
# relative_assets = true